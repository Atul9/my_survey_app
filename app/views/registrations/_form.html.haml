= form_for (@user), url:{action: "create"} do |f|
  = f.label :name
  = f.text_field :name
  = f.label :email
  = f.text_field :email
  = f.label :password_digest
  = f.text_field :password_digest

  .action
    = f.submit
